apiVersion: apps/v1
kind: Deployment
metadata:
  name: core
spec:
  replicas: 3
  template:
    spec:
      containers:
        - name: server
          envFrom:
            configMapKeyRef:
              name: app-config

